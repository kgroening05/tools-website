[project]
name = "tools-website"
version = "0.1.0"
description = "Simple website to host software tools that I build."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.0",
    "jinja2>=3.1.6",
    "markitdown[docx,pdf]>=0.1.6",
    "pypdf>=6.14.2",
    "python-multipart>=0.0.32",
    "uvicorn>=0.51.0",
]
